{
  "name": "@shapegrid/web",
  "version": "0.1.0",
  "private": true,
  "description": "Shapegrid web application - TypeScript frontend with WebGL renderer",
  "type": "module",
  "main": "dist/app.js",
  "types": "dist/app.d.ts",
  "scripts": {
    "build": "tsc && vite build",
    "build:tsc": "tsc",
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/three": "^0.185.1",
    "@types/topojson-client": "^3.1.5",
    "typescript": "^5.4.0",
    "vite": "^8.2.0"
  },
  "dependencies": {
    "three": "^0.185.1",
    "topojson-client": "^3.1.0"
  }
}
